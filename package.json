{
  "name": "tab-disaster-manager",
  "version": "1.0.0",
  "description": "Manifest V3 smart tab manager extension",
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "lint:check": "node -e \"console.log('no lint configured')\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
